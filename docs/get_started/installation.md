# Installation

!!! warning
    This libary can break some functionnalities for the 0.y.z release. The stable version will be released soon.

To install Cruml, add this code into `shards.yml` and run the `shards install` command:
```yaml
dependencies:
  cruml:
    github: tamdaz/cruml
    branch: master
```