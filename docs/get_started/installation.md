# Installation

!!! warning
    This libary can break some functionnalities for each 0.y.z release. The stable version will be released soon.

To install Cruml, add this code into `shards.yml`:
```yaml
dependencies:
  cruml:
    github: tamdaz/cruml
    branch: master
```